---
title: "Test MDX Post - Getting Started with React"
excerpt: "A comprehensive guide to getting started with React.js framework"
thumbnail: "https://docs.1chooo.com/images/cover-with-1chooo-com.png"
author:
  name: Hugo Lin
  avatar: "https://github.com/1chooo.png"
  url: "https://1chooo.com"
publishedAt: "2025-07-11"
category: "Tutorial"
tags:
  - React
  - JavaScript
  - Frontend
  - Web Development
---

# Getting Started with React

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /"Taylor"/#i
const [age, setAge] = useState(50);
const [name, setName] = useState("Taylor");
```

```tsx {6} showLineNumbers
// Avoid
interface Props {
  text?: string;
}

const Heading: React.FC<Props> = ({ text = "Hello, world!" }) => {
  return <h1>{text}</h1>;
};
```

```tsx {6}
// Prefer
interface Props {
  text?: string;
}

function Heading({ text = "Hello, world!" }: Props) {
  return <h1>{text}</h1>;
}
```

## Single Tech Badge Examples

### Using TechBadge Component (Recommended)
<TechBadge badge="typescript" />
<TechBadge badge="react" />
<TechBadge badge="nextjs" />

## Multiple Tech Badges

### Using TechBadgeGroup Component
<TechBadgeGroup badges={["typescript", "react", "nextjs", "tailwindcss"]} />

### Non-clickable Badges
<TechBadgeGroup badges={["python", "django", "docker"]} clickable={false} />

### Custom Styling
<TechBadgeGroup 
  badges={["javascript", "nodejs", "npm"]} 
  className="flex flex-wrap gap-4 mt-4"
/>

This is a test MDX post to demonstrate the experimental blog functionality.

## What is React?

React is a JavaScript library for building user interfaces. It's maintained by Facebook and has become one of the most popular frontend frameworks.

### Key Features

- **Component-Based**: Build encapsulated components that manage their own state
- **Declarative**: Makes your code more predictable and easier to debug
- **Learn Once, Write Anywhere**: Can be used for web, mobile, and desktop applications

## Example Code

Here's a simple React component:

```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}!</h1>;
}

function App() {
  return (
    <div>
      <Welcome name="Sara" />
      <Welcome name="Cahal" />
      <Welcome name="Edite" />
    </div>
  );
}
```

## Conclusion

`CamelCase`

React is a powerful tool for building modern web applications. This MDX blog system allows us to write rich content with code examples and interactive elements.

# GFM

## Nested Lists Examples

### Ordered Lists (Numbers)
1. First item
1. Second item
    1. Nested first
    1. Nested second
        1. Deep nested first
        1. Deep nested second
1. Third item

### Unordered Lists (Bullets)
- First bullet
- Second bullet
    - Nested bullet
    - Another nested bullet
        - Deep nested bullet
        - Another deep nested bullet
- Third bullet

### Mixed Lists (Numbers and Bullets)
1. First numbered item
1. Second numbered item
    - Nested bullet under number
    - Another nested bullet
        1. Deep nested number under bullet
        1. Another deep nested number
    - Back to nested bullet
1. Third numbered item
    1. Nested number under number
        - Deep nested bullet under nested number
        - Another deep nested bullet
    1. Another nested number

### Complex Mixed Example
- Main bullet point
    1. First sub-numbered item
    1. Second sub-numbered item
        - Bullet under number
        - Another bullet under number
            1. Number under bullet under number
            1. Another number under bullet
        - Back to bullet under number
    1. Third sub-numbered item
- Another main bullet point

1. test
1. test
    1. test
    1. test
        1. test

## Autolink literals

www.example.com, https://example.com, and contact@example.com.

## Footnote

A note[^1]

[^1]: Big note.

## Strikethrough

~one~ or ~~two~~ tildes.

## Table

| a | b  |  c |  d  |
| - | :- | -: | :-: |

## Tasklist

* [ ] to do
* [x] done
